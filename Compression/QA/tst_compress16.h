#pragma once

#include <gtest/gtest.h>
#include <gmock/gmock-matchers.h>

using namespace testing;

TEST(Compress16, Compressor)
{
    EXPECT_EQ(1, 1);
    ASSERT_THAT(0, Eq(0));
}


TEST(Compress16, DeCompressor)
{
    EXPECT_EQ(1, 1);
    ASSERT_THAT(0, Eq(0));

}
TEST(Compress16, DeCompressor1)
{
    EXPECT_EQ(1, 1);
    ASSERT_THAT(0, Eq(0));

}
